# Lower bounds here; exact versions are locked in requirements.lock.txt
# (generated by `pip freeze`) so builds are reproducible. SECURITY.md 6.

fastapi>=0.115
uvicorn[standard]>=0.34
psycopg[binary,pool]>=3.2
pydantic>=2.10
pydantic-settings>=2.7
email-validator>=2.2
python-multipart>=0.0.20

# security
argon2-cffi>=23.1        # argon2id password hashing
PyJWT>=2.10              # JWT with a pinned algorithm
Pillow>=11.0             # image validation + re-encode on upload

# tests
pytest>=8.3
httpx>=0.28
